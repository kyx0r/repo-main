data/help is gone upstream; the in-game help text moved to data/core/help.json.
data/CMakeLists.txt still lists the old directory, which makes the install step
fail with:

    file INSTALL cannot find ".../data/help": No such file or directory

Drop the stale entry from the list of data directories to install.

--- a/data/CMakeLists.txt
+++ b/data/CMakeLists.txt
@@ -9,8 +9,7 @@
         title
         core
         screenshots
-        xdg
-        help)
+        xdg)
 
 if (SOUND)
     list(APPEND CATACLYSM_DATA_DIRS sound)
